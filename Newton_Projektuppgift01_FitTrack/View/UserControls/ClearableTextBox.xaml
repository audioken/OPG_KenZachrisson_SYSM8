<UserControl x:Class="Newton_Projektuppgift01_FitTrack.View.UserControls.ClearableTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Newton_Projektuppgift01_FitTrack.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">

    <Grid Background="White">

        <TextBox Text="{Binding UsernameInput, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                 FontSize="16" FontWeight="Light" Background="Transparent"/>

        <Button Width="30" HorizontalAlignment="Right"
                Content="X" Background="Transparent" Foreground="LightGray"
                BorderThickness="0" Command="{Binding ClearTextCommand}"/>
        
    </Grid>

</UserControl>
